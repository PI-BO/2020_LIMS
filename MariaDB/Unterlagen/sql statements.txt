Datenbanken anzeigen:	SHOW DATABASES;

Datenbank benutzen:	USE database_name;

Tabellen anzeigen:		SHOW TABLES;

Table erstellen:		CREATE TABLE table_name (a int);

			CREATE TABLE animals (
			     id MEDIUMINT NOT NULL AUTO_INCREMENT,
			     name CHAR(30) NOT NULL,
			     PRIMARY KEY (id)
			 );
			
			INSERT INTO animals (name) VALUES
			    ('dog'),('cat'),('penguin'),
			    ('fox'),('whale'),('ostrich');

mehrfache Primary Keys:	create table table_name(
			    pk_column1 type,
			    pk_column2 type,
				    ...
			    primary key(pk_column1,pk_column2,)
			);

foreign key + constraint: 	ALTER TABLE projekt_substanz ADD CONSTRAINT contraint_name FOREIGN KEY (projekt_id) REFERENCES projekte(projekt_id);


Tabellen-Attribute anzeigen:	SHOW COLUMNS FROM mytable FROM mydb;

Eintrag in Tabelle:		INSERT INTO person (first_name, last_name) VALUES ('John', 'Doe');
			INSERT INTO tbl_name VALUES (1, "row 1"), (2, "row 2");
			INSERT INTO person SET first_name = 'John', last_name = 'Doe';

Tabelle verändern: 		ALTER TABLE table_name ADD PRIMARY KEY (key_name)

